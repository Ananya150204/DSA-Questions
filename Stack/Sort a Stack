CodeStudio link : https://www.naukri.com/code360/problems/sort-a-stack_985275?leftPanelTabValue=PROBLEM
The problem is how u will insert after calling sort recursively. Think about the base case, i.e., if s is empty or if the element to be inserted
is greater than the top of s. Clearly, we are done. So now apply similar recursion as in sort.
