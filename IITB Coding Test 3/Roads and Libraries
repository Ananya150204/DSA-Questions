https://www.hackerrank.com/contests/iitb-mock-coding-test3/challenges/torque-and-development/problem
Idea : If (c_lib <= c_road) build a library in each city.
else we need to find the connected components in the graph represented by the cities and roads. For each connected component, the cost will be the cost of one library and the cost of roads to connect the cities within the component.
